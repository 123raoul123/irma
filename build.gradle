apply plugin: 'java'
apply plugin: 'application'

mainClassName = "relic.HelloWorld"

sourceSets {
    main.java.srcDir 'src'
    main.resources.srcDir 'resources'
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile 'net.java.dev.jna:jna:4.3.0'
}
